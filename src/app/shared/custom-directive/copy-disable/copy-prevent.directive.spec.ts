import { CopyPreventDirective } from './copy-prevent.directive';

describe('CopyPreventDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyPreventDirective();
    expect(directive).toBeTruthy();
  });
});
