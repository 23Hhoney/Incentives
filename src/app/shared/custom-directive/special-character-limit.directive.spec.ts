import { SpecialCharacterLimitDirective } from './special-character-limit.directive';

describe('SpecialCharacterLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecialCharacterLimitDirective();
    expect(directive).toBeTruthy();
  });
});
